import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
    title: 'Expense Tracker',
    home: Scaffold(
      body: Center(
        child: Text('Init App'),
      ),
    ),
  ));
}
